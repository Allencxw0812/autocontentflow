# 普通人AI实战笔记 — 项目运营手册

## 一、项目简介

**普通人AI实战笔记** 是一个面向普通用户的 AI 工具导航与评测网站。核心理念：**不讲高深概念，只关注"这个 AI 工具能帮你做什么，怎么用最好"。**

### 1.1 核心数据

| 指标 | 数值 |
|------|------|
| AI 工具评测 | 20 个 |
| 使用场景 | 10 个 |
| 用户角色覆盖 | 4 类（职场/自媒体/学生/创业） |
| 工具分类 | 9 个品类 |
| 免费替代方案 | 12 组 |

### 1.2 目标用户

- **职场办公人群** — 用 AI 写周报、做 PPT、整理数据
- **自媒体创作者** — 用 AI 做小红书、剪视频、画封面
- **学生** — 用 AI 辅助论文写作、翻译文献
- **创业者** — 用 AI 降本增效、辅助编程

### 1.3 核心卖点

1. **按场景找工具** — 不需要知道工具名，告诉我们要做什么即可
2. **真实评测** — 每个工具亲自使用，优缺点都写清楚
3. **免费替代** — 付费工具都能找到免费替代方案
4. **可复制提示词** — 每个教程附带可直接使用的提示词模板

---

## 二、功能架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────┐
│                   用户端页面                       │
├────────────┬────────────┬────────────┬────────────┤
│  首页      │ 工具列表页 │ 场景列表页 │  搜索页     │
│  /         │  /tools    │ /scenarios │  /search   │
│ - 热门推荐 │ - 全量工具 │ - 全量场景 │ - 全文搜索 │
│ - 角色导航 │ - 分类筛选 │ - 角色筛选 │ - 热搜标签 │
│ - 最新评测 │ - 免费专区 │            │ - 搜索日志 │
├────────────┼────────────┼────────────┼────────────┤
│ 工具详情页 │ 场景详情页 │ 免费替代   │ 更新日志   │
│ /tools/xxx │ /scenarios │ /free-alt  │ /updates   │
│ - 评测     │ - 工作流   │ - 付费vs   │ - 按月归档 │
│ - 教程步骤 │ - 工具对比 │   免费对照 │ - RSS订阅  │
│ - 同类对比 │ - 避坑指南 │            │            │
│ - FAQ      │ - 实战案例 │            │            │
├────────────┴────────────┴────────────┴────────────┤
│                   辅助页面                         │
│      /about（关于）  /rss.xml（RSS） robots.txt    │
├──────────────────────────────────────────────────┤
│                   管理后台                         │
│     /admin（运营总览）  /admin/search-stats（搜索） │
├──────────────────────────────────────────────────┤
│              自动化运营体系（CI/CD）               │
│  deploy.yml  freshness.yml  auto-review.yml       │
│  ops.mjs（check / refresh / crawl / report）      │
└──────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 框架 | Astro 5 | 静态站点生成，零 JS 运行时 |
| 样式 | Tailwind CSS 4 | 原子化 CSS |
| 搜索 | Pagefind | 静态全文搜索，构建时生成索引 |
| SEO | JSON-LD + Sitemap + RSS | 结构化数据、站点地图、RSS 订阅 |
| 部署 | GitHub Pages | 免费静态托管，push 即部署 |
| AI 自动化 | Claude API | 评测生成、内容刷新、运营建议 |

### 2.3 数据模型

```
tools.json (20个工具)
├── id, name, slug, tagline, description
├── pricing（free/freemium/paid）
├── category（ai-chat/image/writing/code/video 等）
├── rating（coreAdvantage/easeOfUse/chineseFriendly）
├── review（coreAdvantage/painPoints/targetUsers）
├── tutorial（steps + 可复制prompt）
├── similarTools / applicableScenarios / freeAlternativeTo
└── publishedAt / updatedAt

scenarios.json (10个场景 + 4个角色)
├── id, name, slug, tagline, description
├── role（office-worker/content-creator/student/entrepreneur）
├── workflow（3步操作流）
├── comparison（工具横向对比）
├── pitfalls（避坑指南）
├── caseStudy（实战案例）
└── publishedAt / updatedAt
```

---

## 三、运营操作指南

### 3.1 日常运营工作

#### 📝 新增工具

1. 编辑 `src/data/tools.json`，按现有格式添加新工具
2. 填写完整信息：评测、教程、提示词、同类对比
3. 准备工具图标放入 `public/icons/`（PNG，建议 128x128）
4. 本地验证：`npm run build` → 检查页面
5. `git commit` → `git push`，自动部署

#### 🔄 更新评测

**方式一：手动更新**
```bash
# 编辑 tools.json 更新评测内容和 updatedAt 日期
vim src/data/tools.json
npm run build  # 验证
git add -A && git commit -m "update: xxx工具评测更新" && git push
```

**方式二：AI 自动刷新（需要 ANTHROPIC_API_KEY）**
```bash
# 设置 API Key
export ANTHROPIC_API_KEY="sk-ant-xxx"

# 批量刷新所有过期工具评测
node scripts/ops.mjs refresh

# 或只刷新指定过期天数以上的工具
node scripts/ops.mjs refresh --tools

# 预览模式（不实际修改文件）
node scripts/ops.mjs refresh --dry-run
```

#### 📊 查看运营数据

```bash
# 内容新鲜度检查
node scripts/ops.mjs check

# 综合运营周报
node scripts/ops.mjs report

# 爬取所有工具官网，检测 URL 变更/网站故障
node scripts/ops.mjs crawl

# JSON 格式输出（供 CI 消费）
node scripts/ops.mjs report --json
```

访问 `/admin/` 查看可视化的运营后台面板。

### 3.2 自动化运营（GitHub Actions）

系统配置了 3 个自动 Workflow，无需手动干预：

| Workflow | 频率 | 功能 |
|----------|------|------|
| `deploy.yml` | 每次 push | 自动构建 → 部署到 GitHub Pages |
| `content-freshness.yml` | 每周一 | 检查内容新鲜度，过期自动创建 Issue |
| `auto-review.yml` | 每月1日 | AI 批量刷新评测 → 自动提 PR |

**启用 auto-review.yml（AI 自动刷新）：**

在 GitHub 仓库 Settings → Secrets → Actions 添加：
- `ANTHROPIC_API_KEY` — Claude API 密钥

### 3.3 内容运营策略

#### 选题方向

基于搜索数据和热点，优先覆盖以下主题：

1. **免费 AI 工具合集** — 搜索量大，收藏转发率高
2. **AI 工具对比** — "Kimi vs 豆包怎么选" 类长尾 SEO
3. **场景化教程** — "AI 写周报" "AI 做 PPT" 搜索量最高
4. **提示词模板** — 可直接复制使用的提示词最受欢迎

#### SEO 优化清单

- [ ] 每个工具页面有完整的 meta title/description
- [ ] JSON-LD 结构化数据正确（SoftwareApplication + FAQ + Breadcrumb）
- [ ] 工具图标 `public/icons/[id].png` 都存在
- [ ] 场景封面图制作（当前为占位）
- [ ] 文章链接补全（当前 `articles.url` 多为 `#`）
- [ ] 内链优化：工具页 ↔ 场景页互相链接

#### 公众号联动

网站底部配置了公众号引导。运营流程：

1. **网站发布评测** → 公众号推文引用 → 引导搜索和浏览
2. **用户搜索数据** → `/admin/search-stats/` 查看热搜词 → 指导选题
3. **文章系列** → 工具页 `articles` 字段可挂载公众号文章链接

### 3.4 搜索数据分析

网站搜索框已内置搜索日志（localStorage），访问 `/admin/search-stats/` 可查看：

- 总搜索次数 / 独立搜索词 / 近7天 / 今日
- 热门搜索词 TOP 20（带占比）
- 搜索词云
- 自动选题建议
- 导出 JSON 数据

> 注意：数据存储在用户浏览器中，部署后各用户独立。需要汇总数据可接入后端统计。

### 3.5 监控与维护

#### 内容健康度监控

```bash
# 快速检查
node scripts/ops.mjs check

# 示例输出：
# ⚠️ 过期工具: 20 个
# ⚠️ 过期场景: 10 个
# | Kimi 智能助手 | kimi | 2025-07-20 | 318 天 |
```

过期阈值：90 天（可在 `scripts/config.mjs` 修改 `STALENESS_DAYS`）

#### 工具网站可用性监控

```bash
# 检查所有工具官网是否可访问
node scripts/ops.mjs crawl

# 输出：
# - URL 是否跳转、域名是否变更
# - 网站是否宕机
# - 生成 Markdown 报告到 reports/ 目录
```

#### 健康度仪表盘

访问 `/admin/` 查看：
- 内容健康度评分（新鲜百分比）
- 过期内容排行（按天数，红色=严重过期）
- 分类分布 / 定价分布 / 角色覆盖
- 最近更新 TOP 10

### 3.6 命令行速查

```bash
# 构建
npm run build

# 本地预览
node serve.cjs                # http://localhost:4321

# 运营命令
node scripts/ops.mjs check    # 新鲜度检查
node scripts/ops.mjs refresh  # AI 刷新评测
node scripts/ops.mjs crawl    # 爬取官网
node scripts/ops.mjs report   # 综合周报

# 指定范围
node scripts/ops.mjs report --tools        # 仅工具
node scripts/ops.mjs report --scenarios    # 仅场景
node scripts/ops.mjs check --json          # JSON 输出
node scripts/ops.mjs refresh --dry-run     # 模拟运行

# Git 操作
git add -A && git commit -m "update: xxx" && git push
```

---

## 四、项目文件结构

```
ai-website/
├── .github/workflows/        # CI/CD 自动化
│   ├── deploy.yml            # GitHub Pages 部署
│   ├── content-freshness.yml # 每周内容新鲜度检查
│   └── auto-review.yml       # 每月 AI 自动刷新
├── public/                   # 静态资源
│   ├── icons/                # 工具图标 (20个PNG)
│   ├── images/               # 图片素材
│   ├── _headers              # 安全头 + 缓存策略
│   └── _redirects            # URL 重定向
├── scripts/                  # 运营自动化脚本
│   ├── ops.mjs               # 编排主脚本（4个子命令）
│   ├── config.mjs            # 全局配置
│   └── lib/
│       ├── ai.mjs            # Claude API 调用
│       ├── crawler.mjs       # 网页爬虫
│       └── data.mjs          # 数据读写
├── src/
│   ├── components/           # Astro 组件 (15个)
│   ├── data/                 # 数据文件
│   │   ├── tools.json        # 工具数据 (20个)
│   │   └── scenarios.json    # 场景+角色数据
│   ├── layouts/              # 页面布局
│   ├── pages/                # 页面路由
│   │   ├── admin/            # 管理后台 (运营总览+搜索统计)
│   │   ├── tools/            # 工具列表+详情
│   │   ├── scenarios/        # 场景列表+详情
│   │   └── ...               # 其他页面
│   ├── styles/               # 全局样式
│   ├── types/                # TypeScript 类型
│   └── utils/                # 工具函数
├── reports/                  # 运营报告输出 (gitignore)
├── astro.config.mjs          # Astro 配置
├── serve.cjs                 # 本地预览服务器
└── wrangler.toml             # Cloudflare 配置（备用）
```

---

## 五、常见问题

**Q: 如何批量更新所有工具的 updatedAt 日期？**
```bash
node -e "
const {loadTools, saveTools} = require('./scripts/lib/data.mjs');
const tools = loadTools();
tools.forEach(t => t.updatedAt = new Date().toISOString().split('T')[0]);
saveTools(tools);
console.log('All 20 tools updated');
"
```

**Q: 搜索功能在本地不工作？**
Pagefind 索引在 `npm run build` 时生成，本地开发用 `npm run dev` 不会生成搜索索引。需要先 build 再用 `serve.cjs` 预览。

**Q: GitHub Pages 部署后样式错乱？**
检查 `astro.config.mjs` 中 `base` 是否与仓库名一致（当前是 `/autocontentflow/`）。

**Q: 如何接入百度统计？**
编辑 `src/layouts/BaseLayout.astro`，取消 analytics 占位注释，替换为百度统计代码。
